// use dao::entity::t_ds_user::Entity as User;
use sea_orm::*;
#[tokio::main]
async fn main() {
    // let db = DbConn::new();
    // let mut users = User::find().all().await.unwrap();
    println!("Hello, world!");
}
